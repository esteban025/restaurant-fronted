<section>
  <div class="hero relative">
    <div class="aspect-[16/7] w-full">
      <video id="hero-video" class="w-full object-cover h-full" autoplay loop muted>
        <source src="./hero-restaurante-video.mp4" type="video/mp4" >
        Tu navegador no soporta el video HTML5.
      </video>
    </div>
    <div class="absolute inset-0 flex items-center justify-center bg-black/60">
      <h1 class="font-bold text-white font-corinthia text-center">
        <span class="block text-2xl ssfi">Bienvenido</span>
        <span class="block text-7xl ssse">Punto del Sabor</span>
      </h1>
    </div>
  </div>
</section>

<style>
  .ssfi {
    font-size: clamp(1.5rem, 2vw + 1rem, 2.5rem);
  }
  .ssse {
    font-size: clamp(3rem, 5vw + 1rem, 7rem);
  }
</style>

<script>
  const video = document.getElementById('hero-video') as HTMLVideoElement;
   video.playbackRate = 0.65;
</script>